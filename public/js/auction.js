
const sumBtn = document.getElementById('submit-button')

sumBtn.addEventListener("click", (event) => {
    const textBox = document.getElementById('submit-box')
    const bid = textBox.value
    fetch 
});