<section class="container">
  <h3 class="product-list">Hypochondriacs</h3>
  {{!-- <h4>Opening Hour: {{format_time auction.starting_date}}</h4>
  <h4>Closing Hour: {{format_time auction.ending_date}}</h4> --}}

  {{!-- {{#if loggedIn}} --}}
  <section class="flex-row justify-center">
    {{#each products as |product|}}
    {{> product-details}}
    {{/each}}
  </section>
</section>

{{!-- {{else}} --}}
<button id="mustLogin">You must login to bid on the products</button>
{{!-- {{/if}} --}}

{{!-- TODO: missing /js/product.js  --}}
<script src="/js/product.js"></script>