<section class="container">
  <h1 class="product-list"></h3>

  <section class="flex-row justify-center">
    {{#each products as |product|}}
    {{> product-details}}
    {{/each}}
  </section>
</section>

